<h1>Weather definitions</h1>

<div>

  <% if @hot %>
    <div>
      Above <%= @hot.value %><span>&#8451;</span> is <span class="text-danger">Hot</span>.
    </div>
  <% end %>

  <% if @hot and @cold %>
   <div>
     Between <%= @cold.value %><span>&#8451;</span> and <%= @hot.value %><span>&#8451;</span> is <span class="text-warning">Warm</span>.
   </div>
  <% end %>

  <%if @cold %>
    <div>
      Below <%= @cold.value %><span>&#8451;</span> is <span class="text-info">Cold</span>.
    </div>
  <% end %>

  <% unless @hot and @cold %>
    No/Incomplete Temperature Information.
  <%end%>
</div></br>

<div>
  Please set temperature definitions for hot and cold temperatures <a href="<%= new_temperature_path %>">here</a>
</div>

</br>

<h2> Search weather by postcode</h2>
<%= form_for :search, url: forecasts_search_path do |form| %>
  <div>
    <%= form.label :postcode %>: &nbsp;
    <%= form.text_field :postcode %>
  </div></br>

   <div>
    <%= form.submit 'Search'%>
  </div>
<% end %>

<% if @country && @maxtemp %>
  <h3> Temperature Results </h3>
  <div>
    Country: <%= @country %></br>
    Max.Temp: <%= @maxtemp %><span>&#8451;</span>
  </div>
<% end %>
